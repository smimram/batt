is-contr :: U → U
is-contr A = Σ (a : A) . (x : A) → (a ≡ x)

center :: (A : U) → is-contr A → A
center A (a, _) = a

HContr :: TYPE
HContr = Σ (A : U) . is-contr A

type-HContr :: HContr → U
type-HContr (A, _) = A
