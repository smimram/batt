-- a boolean
b : Bool = false

-- a pair of booleans

bb : Bool × Bool = (false , true)

-- identity on bool
idBool : Bool -> Bool = fun x -> x

-- polymorphic identity
id : (A : U) -> A -> A = fun AA -> fun x -> x
