-- a boolean
b : Bool
b = false

-- a pair of booleans
bb : Bool × Bool
bb = (false , true)

bb' : Bool ⨂ Bool
bb' = (false ⊗ true)

app : (A B : U) → (A → B) → A → B
app = fun A B f x → f @ x

not : Bool → Bool
not = Bool_ind(true,false)

cstf : Unit → Bool
cstf = Unit_ind(false)

-- -- terminal arrow
-- term : (A : U) → A → Unit
-- term = fun A → fun x → tt

-- unflatten : (A :: U) → ♭A → A
-- unflatten = fun A → fun a → ♭_ind(x, A, x, a)
-- unflatten = fun A → fun a → let♭ x = a in x

-- should not work
-- flatten : (A :: U) → A → ♭A
-- flatten = fun A → fun a → 𝄫a

-- identity on bool
idBool : Bool → Bool
idBool = fun x → x

-- polymorphic identity
id : (A : U) → A → A
id = fun A x → x

-- fstb : Bool ⨂ Bool → Bool
-- fsb =

-- linear swap
-- swap : (A B : U) → A ⊗ B → B ⊗ A
-- swap = fun A B →
