-- testing let

n : bool
n =
  let id : (A : Type) → A → A = λ A x → x in
  id bool false

n : bool
n =
  let id : {A : Type} → A → A = λ x → x in
  id false

n : bool × bool
n =
  let b : bool = false in
  (b , b)
